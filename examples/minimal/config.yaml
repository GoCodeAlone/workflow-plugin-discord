# Minimal config exercising workflow-plugin-discord.
# Schema-validate with: wfctl validate --skip-unknown-types examples/minimal/config.yaml
version: 1
modules:
  - name: discord-provider
    type: discord.provider
    config: {}
workflows:
  pipeline:
    steps:
      - name: example
        type: step.discord_send_message
        config: {}
